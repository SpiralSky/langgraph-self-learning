# LangGraph Configuration
NEXT_PUBLIC_API_URL=http://localhost:2024
# The `chat` graph (see backend/langgraph.json) streams `messages`-mode events,
# which is what this assistant-ui chat requires. The `agent` graph is
# values-mode and renders nothing in the chat view.
NEXT_PUBLIC_ASSISTANT_ID=chat
# Optional. Required for Agent Builder deployments.
# Set to "langsmith-api-key" when using a LangSmith PAT.
NEXT_PUBLIC_AUTH_SCHEME=
# Do NOT prefix this with "NEXT_PUBLIC_" as we do not want this exposed in the client.
LANGSMITH_API_KEY=

# Separate feedback FastAPI (POST /feedback). The frontend proxies
# same-origin POST /api/feedback here (see src/app/api/feedback/route.ts).
NEXT_PUBLIC_FEEDBACK_API_URL=http://localhost:8000

# Production LangGraph Configuration (quickstart) - Uncomment to use
# NEXT_PUBLIC_ASSISTANT_ID="agent"
# This should be the deployment URL of your LangGraph server
# LANGGRAPH_API_URL="https://my-agent.default.us.langgraph.app"
# This should be the URL of your website + "/api". This is how you connect to the API proxy
# NEXT_PUBLIC_API_URL="https://my-website.com/api"
# LANGSMITH_API_KEY="lsv2_..."
