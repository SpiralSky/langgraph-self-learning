[pytest]
testpaths = tests
pythonpath = src
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    integration: marks tests needing real LLM/mem0
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
